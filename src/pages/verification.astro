---
import Layout from "../layouts/base.astro";
import { Icon } from "astro-icon/components";
---

<Layout title="Vérificaion">
  
  <div class="flex flex-col h-screen justify-center items-center">

    <div class="card card-dash bg-base-100 border-base-300 w-96 md:w-110 mt-2">
      <div class="card-body w-full">
        <h2 class="card-title text-2xl justify-center">
          Inscription Réussie 🥳
        </h2>
        <p>
          Si vous voyez ce message, c'est que votre inscription a été validée
          avec succès !
        </p>
        <p>
          <strong>Vous devez vérifier votre compte pour pouvoir utiliser l'app !</strong> Nous vérifions les comptes manuellement pour vérifier que vois venez de Kernanec.
        </p>
        <fieldset class="fieldset bg-base-200 border border-base-300 p-4 rounded-box">
          <legend class="fieldset-legend text-lg">Votre Identifiant</legend>
          <p class="text-center text-4xl -translate-y-2" id="uid">
          </p>
          <p class="text flex justify-center text-center">
            <Icon name="mdi:information-slab-circle" class="text-lg"/>
            Gardez une capture d'écran de cette page pour votre vérification</p>
        </fieldset>
        <p>
          Montrez cette page à un AED ou membre de l'équipe PokéKerna pour faire vérifier votre compte.
        </p>
        <div class="card-actions justify-center pt-2">
          <a class="btn btn-secondary" href="/roadmap">
            <Icon name="mdi:calendar-clock" class="text-3xl" />
            Roadmap
          </a>
          <a class="btn btn-success" href="/aide">
            <Icon name="mdi:check" class="text-3xl" />
            Aide Vérification
          </a>
        </div>
      </div>
    </div>
  </div>
  <script is:inline>
    const param = new URLSearchParams(window.location.search).get('uid');
      document.getElementById('uid').textContent = param
        ? param
        : '❌';
    console.log(new URLSearchParams(window.location.search));
  </script>
</Layout>
